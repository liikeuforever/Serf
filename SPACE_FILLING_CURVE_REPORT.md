# 空间填充曲线压缩 - 测试报告

## 实现概述

本项目基于Serf-QT的差值编码思想，实现了三种空间填充曲线压缩方法：

1. **标准 GeoHash**: 全球坐标系统，经纬度交错编码
2. **MBR优化 GeoHash**: 基于数据集边界框（MBR）的Z-Order曲线
3. **Hilbert 曲线**: 基于数据集边界框的Hilbert空间填充曲线

## 关键修正与验证

根据专家建议，进行了以下重要修正：

### 1. 修正归一化逻辑 ✅
**问题**: 原使用 `* (n - 1)` 进行量化，导致边界附近量化偏差  
**后果**: MBR边缘点被错误映射到同一整数坐标，精度严重下降  
**修正**: 改为 `* n` 量化，配合 `min(n-1, x)` 边界处理

```cpp
// 修正前（错误）
int x = static_cast<int>(norm_lon * (n - 1));

// 修正后（正确）
int x = static_cast<int>(norm_lon * n);
x = std::min(n - 1, x);  // 防止越界
```

### 2. Hilbert 曲线实现验证 ✅
**实现来源**: 基于 John Skilling (2004) "Programming the Hilbert curve" 和 Wikipedia  
**验证方法**: 
- 4x4网格测试：所有编码0-15都存在且唯一 ✓
- 编码/解码可逆性：1024x1024网格测试通过 ✓
- 空间局部性：相邻点编码差值合理 ✓

**Rot函数说明**: 当 `ry == 1` 时不需要旋转是正确的算法行为，不是bug！

### 3. 简化 MBR-GeoHash 实现 ✅
**初版问题**: 根据宽高比动态分配比特数，导致解码复杂且容易出错  
**最终方案**: 使用标准位交错（偶数位=经度，奇数位=纬度），简单可靠  
**验证**: 解码精度从2.13度降到6.77e-08度 ✓

## 测试结果

### 数据集信息
- **数据集**: GeoLife 100k轨迹数据
- **点数**: 100,000
- **边界框**: 
  - 经度: [116.201597°, 121.470629°] (宽度: 5.269°)
  - 纬度: [31.167749°, 40.081138°] (高度: 8.914°)
  - 宽高比: 0.59
- **精度要求**: 最大误差 ≤ 1e-5 度

### 压缩效果对比

| 方法 | 压缩大小 | 压缩比 | 平均编码 | 最大误差 | 差值范围 |
|------|---------|--------|---------|---------|---------|
| **标准 GeoHash** | 285,161 bytes | **5.61:1** | 22.81 bits/点 | 1.93e-06 度 ✅ | [-14T, 91B] |
| **MBR GeoHash** | 734,443 bytes | 2.18:1 | 58.76 bits/点 | 6.77e-08 度 ✅ | [-2927T, 1501T] |
| **Hilbert 曲线** | 730,258 bytes | 2.19:1 | 58.42 bits/点 | 6.77e-08 度 ✅ | [-4417T, 3706T] |

*注: T = 万亿 (trillion), B = 十亿 (billion)*

### 关键发现

#### 1. 标准GeoHash压缩比最好 (5.61:1)
**原因**:
- 全球坐标系统，编码值在整个uint64范围内分布
- **相邻轨迹点的编码值差异很小**
- 例如前两个点: 4068801695285153 → 4068801695306828 (差值仅21675)
- 7.1%的点差值为0（完全相同的格子）

#### 2. MBR和Hilbert压缩比较低 (2.18:1, 2.19:1)
**原因**:
- 虽然局部精度更高（误差仅几百万分之一度）
- 但编码值在整个52位空间分散
- **差值跳跃巨大**（数万亿级别）
- 仅5.3%的点差值为0

#### 3. 精度对比
所有三种方法都满足 1e-5 度的精度要求：
- 标准GeoHash: 1.93e-06 度（格子对角线 6e-06 度）
- MBR GeoHash: 6.77e-08 度（格子对角线 1.54e-07 度）
- Hilbert曲线: 6.77e-08 度（格子对角线 1.54e-07 度）

## 核心技术细节

### 编码流程
1. **空间编码**: 经纬度 → 一维整数（GeoHash/Hilbert）
2. **差值计算**: `diff = current_code - previous_code`
3. **ZigZag编码**: 将有符号整数映射到无符号（小值→小编码）
4. **Elias Gamma**: 可变长度编码（值越小，编码越短）

### 精度计算
- 52位编码在全球范围：格子对角线 ≈ 6e-06 度
- 52位编码在MBR范围（5.27° × 8.91°）：格子对角线 ≈ 1.54e-07 度
- MBR方案精度提升约**40倍**

### 为什么标准GeoHash更好？

对于**轨迹数据**：
1. ✅ 相邻点空间距离小 → 编码值差异小 → 差值编码效果好
2. ✅ 7.1%点差值为0（1 bit编码）
3. ✅ 大部分差值在小范围内 → Elias Gamma编码高效

MBR和Hilbert虽然精度更高，但：
1. ❌ 编码空间利用率低
2. ❌ 差值跳跃大（轨迹连续性在一维编码中体现差）
3. ❌ Elias Gamma编码长度增加

## 与 Serf-QT 对比

Serf-QT使用**运动矢量预测**：
- 优点: 针对轨迹特性优化，零校正率高
- 缺点: 实现复杂，参数调优困难
- 精度问题: 1e-5度的误差限制对Serf-QT过于严格（量化参数需要极小）

空间填充曲线方法：
- 优点: 实现简单，稳定可靠，精度可控
- 缺点: 不如Serf-QT针对性强
- 适用场景: 通用的轨迹压缩，对精度有严格要求

## 结论

1. **标准GeoHash + 差值编码** 在GeoLife数据集上表现最优（5.61:1）
2. **精度修正至关重要**: `* n` 量化避免边界偏差
3. **局部优化不一定全局最优**: MBR虽然精度高，但差值编码效果差
4. **轨迹数据特性**: 空间连续性 → 编码连续性 → 高压缩比

## 建议

### 实用场景选择
- **推荐**: 标准GeoHash + 差值编码
- **高精度需求**: Hilbert曲线（精度提升40倍，代价2.5倍压缩率）
- **运动预测场景**: Serf-QT（需要放宽误差限制）

### 未来改进方向
1. 自适应编码比特数（根据数据密度）
2. 分段压缩（不同区域不同策略）
3. 混合方法（运动预测 + 空间编码）
4. 多级精度编码（重要点高精度，普通点低精度）

## 文件清单

### 核心实现
- `src/compressor/space_filling_curve.h/cc` - 空间填充曲线编码
- `src/compressor/sfc_compressor.h/cc` - 差值压缩器
- `src/compressor/sfc_decompressor.h/cc` - 解压缩器

### 测试程序
- `space_filling_curve_compression_test.cc` - 精度验证测试
- `simple_sfc_test.cc` - 压缩效果对比测试
- `comprehensive_sfc_test.cc` - 完整测试（含解压）

### 数据
- `test/data_set/Geolife_100k_longitude_latitude.csv` - 测试数据集

---

**测试日期**: 2025-10-09  
**数据集**: GeoLife 100K  
**实现**: C++17, 基于Serf项目

